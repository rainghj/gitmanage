# 2026-09-01

## gitmanage 项目启动

- 目标：做一个类似 IDEA Git Tool Window 的轻量 Git 管理器，占用系统资源少，仅面向 Windows 11。
- 技术选型（已确认）：Tauri v2 + React/TS + git2-rs（libgit2，default-features=false 关闭 ssh/https/openssl，v0.1 只做本地仓库操作；远程推拉后续用 git CLI 侧车）。
- 布局：对齐 IDEA 截图——顶部仓库栏 + 左侧文件树/工作区更改 + 中间分支行+提交历史 + 右侧改动文件+diff。VS Code 风格深色蓝灰调。

## 环境准备（已完成）

- Rust 1.98.0 (stable-x86_64-pc-windows-msvc) 装在 C:\Users\guohj\.rustup + .cargo
- VS Build Tools 2026（"使用 C++ 的桌面开发"工作负载），装在 C:\Program Files (x86)\Microsoft Visual Studio\18\BuildTools，占用 6.76GB
- cargo build hello-world 链接验证通过
- C 盘实际剩余 217GB（早前记忆里"只剩 6.2GB"已过时，不用再做磁盘重定向）
- 注意：bash 环境 PATH 里没有 cargo，构建时需 `export PATH="/c/Users/guohj/.cargo/bin:$PATH"`

## 项目骨架进度

- create-tauri-app 初始化完成（react-ts 模板，pnpm）
- package/productName/identifier 改为 gitmanage / GitManage / com.guohj.gitmanage，窗口 1400x900
- Rust 命令桥已实现：open_repo / list_branches / get_log / get_commit_files / get_diff / get_head_tree / get_status
- 前端三栏 + diff 渲染完成，tsc 通过
- 首次 cargo build（全量依赖编译）进行中

## 首轮收尾（同日）

- Rust 编译修过 4 个错：head 借用阻止 repo move（块作用域解决）、upstream Option<&str>→Option<String>、commit_diff 缺生命周期参数、git2 0.20 的 Diff::print 回调 delta 是 DiffDelta 而非 Option。
- pnpm 11 坑：ERR_PNPM_IGNORED_BUILDS 会让 install 退出码=1，连锁导致 pnpm build 前置检查失败。解法：pnpm-workspace.yaml 的 `allowBuilds: esbuild: true`（pnpm 自动生成模板文件，填 true 即可）。
- 前端 pnpm build 通过（JS 199KB / gzip 63KB）；cargo build 通过。
- `pnpm tauri dev` 已启动，gitmanage.exe 运行中，内存约 34.7MB（debug 含 devtools），对照 IDEA 的 GB 级达成"轻量"目标。
- gitmanage 目录已 git init（main 分支），首个提交"初始化：Tauri v2 + React + git2-rs 骨架"，可作为应用自身的测试仓库。
- 待办：用户在窗口里验证 UI/数据；后续分支操作（checkout/merge）、分支图绘制、大仓库分页/虚拟滚动。

## 第二轮：分支操作 + 提交（已完成）

- Rust 新增命令：checkout_branch / create_branch / delete_branch / stage_all / commit。
  - delete_branch 用 graph_descendant_of 判断合并（git2 Branch 没有 is_merged）
  - commit 用 repo.signature()，无 HEAD 时作为根提交
- 前端：本地分支 chip 可点击 checkout、非当前分支带 × 删除、"＋新分支"、工作区有改动时出现提交框（自动 stage_all 再 commit）。
- 编译状态：tsc 通过；tauri dev 自动重编出 gitmanage.exe（17:36），运行内存 34.3MB。
- 教训：tauri dev 已在监听并自动重编译时，不要再手动跑 cargo check/build——会抢文件锁互相阻塞，白等近 10 分钟。用 target 产物时间戳判断是否已完成。
- git 已提交两版：5e7d0bf（骨架）、129e0d2（分支操作与提交）。

## 第三轮：文件夹选择对话框

- 需求："打开仓库"改为可弹出系统文件夹选择器自选目录。
- 方案：tauri-plugin-dialog v2（官方插件）。三处必需改动：
  1. 前端 `pnpm add @tauri-apps/plugin-dialog`，`import { open } from "@tauri-apps/plugin-dialog"`
  2. Rust Cargo.toml 加 `tauri-plugin-dialog = "2"` + lib.rs `.plugin(tauri_plugin_dialog::init())`
  3. **capabilities/default.json 的 permissions 必须加 "dialog:default"**（插件默认权限不在 core:default 里，漏了会运行时报错）
- `open({ directory: true, multiple: false, defaultPath })` 返回 string|null，取消时为 null。
- 验证：exe 17:54:20 重编（15.6MB→16.4MB），新进程内存 31.9MB。
- 校验编译完成的方法：看 target/debug/gitmanage.exe 时间戳是否晚于源码，以及 rustc/link.exe 是否归零。

## 第四轮：分支图 lane 连线 + discover 容错

- open_repo 改用 `Repository::discover(path)` 向上逐级查找 .git（选中仓库子目录也能打开），失败回退 `Repository::open`，再失败才报中文错误；返回真实仓库根（trim 结尾分隔符），前端回填输入框。
- 分支图：**纯前端计算**（不改 Rust，用已有的 parents 数组），在 App.tsx 里 computeGraph 维护 lanes 泳道数组：
  - commit 已在某泳道则沿用，否则占空泳道/新开
  - 第一父接手当前泳道；merge 的第二父另开泳道 → 产生分叉/汇合
  - 每行快照 occupied 画竖线，merge 行画贝塞尔汇合曲线，节点画圆点
  - 泳道配色用 VS Code 色板（无紫色）：#569cd6 #4ec9b0 #d7ba7d #ce9178 #b5cea8 #9cdcfe
- 关键耦合：SVG 行高 GRAPH_ROW_H(46) 必须与 CSS .commit-item 的 height(46px) 一致，否则竖线接不上。
- 提交 85d2809。

## tauri dev 死灰复燃坑（当场处理）

- 触发条件：用户在截图里看到应用窗口是 Edge 的"ERR_CONNECTION_REFUSED / localhost 拒绝连接"。
- 根因：Rust 重编译完成后 `tauri dev` 主进程没有正确派生 `pnpm dev` (vite) 的重启，vite 进程异常退出但 tauri 主进程还活着，WebView 指向死地址。
- 排查方法：
  1. `curl -s -o /dev/null -w "%{http_code}" http://localhost:1420` → 看是 200 还是 502/无响应
  2. `tasklist` 看 node.exe 是否存在
  3. Git Bash 的 `</dev/tcp/127.0.0.1/1420` 在 Windows 下不准（会显示 CLOSED），**以 curl 为准**
- 恢复方案：杀干净重启——
  ```bash
  # Git Bash 下（注意用 cmd 转发，taskkill 的 // 会报错）
  cmd //c "taskkill /F /IM gitmanage.exe & taskkill /F /IM node.exe"
  # 或者 PowerShell：
  #   Get-Process gitmanage,node | Stop-Process -Force
  sleep 2 && pnpm tauri dev  # 后台跑
  ```
- 经验：以后长时间 dev 中如果窗口变 ERR_CONNECTION_REFUSED，几乎都是这个原因。

## 第五轮：提交过滤 + 搜索

- 需求：按分支 + 关键字（匹配 hash / 作者 / message）过滤提交历史，IDEA Git Log 的核心交互。
- Rust：`get_log(limit, branch: Option<String>, query: Option<String>)`
  - branch=None/空/"HEAD" → push_head；其余按 revparse_single 解析
  - query 小写包含匹配，作用于 oid + author + summary 拼接串
  - revwalk 上限 10000 条兜底，防止不匹配 query 遍历整库；limit 在过滤后达到即 break
  - 注意：filter 后的 limit 语义变成"前 N 条匹配项"（不是"前 N 条中过滤"）
- 前端：
  - filterBranch / filterQuery 两个 state
  - useDebounced(value, 300) hook 实现 300ms 防抖（避免逐字符发 RPC）
  - filter-toolbar 栏：select（全部 + 各分支，远程也带 ☁ 前缀） + search input + 清除按钮
  - commit count 标签里直接显示过滤后的数量
- tsc 通过、Rust 自动重编 18:15 完成、新版 exe 16.4MB、app 36.8MB。

## 第六轮：「最近打开」仓库列表

- 需求：欢迎页中央展示最近打开的仓库，快速重开。
- 用户选定方案：AppData 下的 JSON 文件 + 上限 10 条。
- Rust：
  - 文件位置：`%APPDATA%\com.guohj.gitmanage\recent.json`（用 `app.path().app_config_dir()`）
  - 三个 Tauri 命令：get_recent / add_recent / remove_recent，都接收 `tauri::AppHandle` 参数
  - 大小写不敏感 dedupe 同路径，按 last_opened 排序，超过 10 条截断
- 前端：
  - 首页加 recent 列表：图标 + 项目名 + 路径 + 相对时间（刚刚/N分钟前/昨天/日期）
  - 每行 hover 高亮 + × 删除按钮（事件 stopPropagation 不触发打开）
  - 打开仓库成功后异步调 add_recent（失败不影响主流程）
  - 没打开仓库时拉一次最近列表
- **踩到的 Tauri 2 API 坑**：
  - `app.path()` 方法来自 `tauri::Manager` trait，必须 `use tauri::Manager;`
  - 直接 `tauri::Manager::path()` 这种函数调用式写法会编译失败
  - 需要 app handle 的命令函数加 `app: tauri::AppHandle` 参数
- 编译 + 启动流程：tauri dev 这次没自动重编成功（rustc 已归零但 exe 未更新）—— 杀干净 → 手动 cargo build → 启动 tauri dev 才正常。**这之前提过类似坑，再次坐实**。

## 第七轮：远程操作（fetch / pull / push）

- 选型：git2 默认关闭了 https/ssh，所以走 **git CLI 侧车**（std::process::Command）
- 优势：复用 Windows 凭据管理器 + SSH agent + 已装的 git，不引入 openssl-sys 重型依赖
- Rust：`git_remote_op(op, remote: Option, branch: Option) -> Result<String, String>`
  - op ∈ {"fetch","pull","push"}，默认 remote="origin"
  - 关键环境变量 `GIT_TERMINAL_PROMPT=0` —— 防止 git 在后台弹交互式凭据输入把界面卡死
  - 检测 Authentication failed / Permission denied 等关键字时给出"凭据未提供"提示，引导用户用 Windows 凭据管理器或 SSH
  - 合并 stdout + stderr 一起返回（git 的进度信息走 stderr）
- 前端：
  - 顶部右侧加按钮组 `↓ fetch / ↓ pull / ↑ push / ▲ Console`，调用自动展开面板
  - 底部可折叠 Console 面板（200px 高，黑底 monospace），fetch/pull/push 输出的原始 git 输出都在这里
  - 操作期间按钮禁用并显示 …
  - 成功后自动 refresh() 拉新分支和提交
- 编译流程又出怪事（rustc 归零但 exe 未更新），改用 kill → cargo build → pnpm tauri dev 的稳态三步走。
- 提交 242089e 之后继续。

## 第八轮：Stash 操作

- 选型：本地操作，直接用 git2 原生 stash API（stash_save / stash_foreach / stash_apply / stash_pop / stash_drop），比启 CLI 进程更快。
- Rust：
  - 5 个命令：stash_list / stash_save / stash_apply / stash_pop / stash_drop
  - stash_save 用 StashFlags::INCLUDE_UNTRACKED（包含新增未跟踪文件）
  - signature_or_default 兜底：仓库没配 user.name/email 时用 `GitManage <gitmanage@local>` 顶替，避免直接报错
- **踩坑**：git2 的 stash_* API 需要 `&mut Repository`，但之前 `with_repo!` 宏给的是 `&`。修法：宏统一改为 `as_mut()` 拿 `&mut Repository`——不可变 API 接受 `&mut` 会自动 reborrow 为 `&`，统一可变更省心
- 前端：
  - 状态栏 Stash 区块（左栏底部）：每项三按钮（apply / pop / drop），全 confirm 防误删
  - 提交框多一个 "Stash 暂存" 按钮（仅在工作区有改动时出现）
  - refresh() 同时拉 stash_list
- 编译又走稳态三步：kill → cargo build（2m25s）→ pnpm tauri dev

## 验证回访：欢迎页「最近打开」已生效

- 用户截图回访：`recent-list` 已正常显示——gitmanage 与 agent-hub 两个仓库均带路径、时间、删除按钮。
- 代码复核：上一轮（第六轮）就已经把 get_recent / add_recent / remove_recent + welcome 区块写完，只是当时没有用户截图确认，截图一发即可看到效果。
- 本轮无需改动代码。

## 第九轮：最近打开仓库的快捷工具 + 失效检测

接着让这块更扎实——两个真实痛点：
1. 仓库被移动/删除后点开就直接报错，没有视觉提示
2. 列表项只能打开/删除，缺两个常用动作：在资源管理器定位 / 复制路径

**Rust 改动**：
- `RecentEntry` 增字段 `exists: bool`，加 `#[serde(default)]` 让旧版 recent.json 还能继续解析（不会把历史一次性清空）
- `get_recent` 在返回前对每条 `Path::new(&e.path).exists()` 重新算 exists（不调用 git2，免得坏仓库红字）
- `add_recent` 新插入的条目 `exists: true`（刚打开必存在）
- 新增命令 `reveal_in_explorer(path)`：用 `explorer /select,<path>`（文件则回退到父目录），spawn 后即返回——explorer 是软失败进程，exit code 不可靠
- 注册到 `invoke_handler!`

**前端改动（App.tsx）**：
- `RecentEntry` TS 类型加 `exists`
- 列表项 main 按钮增加 disabled 态判断：不存在时点击弹错误而不是尝试打开
- 缺路径状态：图标 📁→⚠、路径加删除线、time 列变红色"不存在"
- 新增 `.recent-tools` 工具栏（默认 opacity:0，hover/focus 淡入）含三个按钮：
  - 📂 在 Windows 资源管理器中打开（走 `reveal_in_explorer` Rust 命令）
  - ⧉ 复制路径（走 `navigator.clipboard.writeText`）
  - × 删除（保留原行为）
- 所有工具按钮 `e.stopPropagation()` 不触发 main 按钮的打开

**CSS 改动（App.css）**：
- `.recent-tools`：默认隐藏，hover/focus-within 显示
- `.recent-tool`：灰底透明 hover 变蓝（匹配 accent 主题）
- `.recent-item.missing`：暗红描边 + 浅红背景 + 文字删除线

**编译流程**：user 给截图时跑着 dev，所以走经典稳态三步——
1. PowerShell `Stop-Process -Force` 杀 gitmanage/cargo/rustc/node（cmd taskkill 因 UTF-8 中文字节头挂掉，记下）
2. `cargo build`（src-tauri 下，3m34s，全过）
3. tsc 通过、然后 `pnpm tauri dev` 拉起（`/c/Users/guohj/.cargo/bin:$PATH` 必须前置，bash 环境默认没 cargo）
- tauri dev 又复现了 `cargo metadata` PATH 缺失（前面 `pnpm tauri dev 2>&1` 那次先挂了）——这条已经踩过 3 次了，**新工作流里直接 export PATH 后再 pnpm tauri dev**。

启动后 `tasklist` 看 `gitmanage.exe` 内存 48MB（含 devtools），正常。

## 第十轮：关闭仓库 + 最近下拉菜单

发现真实缺口：打开仓库后**没有任何路径回到欢迎页**，且 Rust 侧如果不清空 Repository 会一直占着 .git 文件锁（Windows 上其他 git 操作/资源管理器删除会被拒）。一次性补两个能力：

**Rust 改动**：
- 新命令 `close_repo`：把 `state.repo` 置 None 以释放 git2 句柄
- 注册到 invoke_handler

**前端改动**：
- 抽出 `loadRecent = useCallback(...)`，欢迎页 + 顶部栏下拉都共用
- effect `[repo, loadRecent]` 在挂载 / 打开 / 关闭三种时机都拉一次最近列表
- `openRepo` 在 `add_recent` 成功后追加调一次 `loadRecent`（open 新仓库后下拉菜单立刻反映最新顺序）
- 新加 `closeRepo()` 函数：先 await `close_repo` 释放后端句柄（失败就 return，不继续否则前后端脱钩），再清空所有相关 state（branches/commits/tree/status/stashes/selected/files/diff/filter*/commitMsg/console*/repoPath/recentMenuOpen）
- 新 state `recentMenuOpen` + ref + effect：点击下拉外部关闭

**Topbar 新增两个按钮**（仅在已开仓库时显示）：
- 「最近 ▾」下拉：宽 360px、深色卡片、当前仓库用 ● + 蓝底高亮、缺失路径灰显 + 删除线、底部「✕ 关闭当前仓库」红色快捷动作
- 「关闭」按钮：直接调 `closeRepo`

**CSS**：
- `.recent-menu-wrap/.recent-menu/...` 一整套：定位、阴影、滚动、hover 高亮、current 选中态、missing 灰显、底部分隔线

**编译/启动**：
- 这次 `loadRecent` 在 useCallback 里调用、但定义在它后面，tsc 报 `used before declaration`（行 390）——把 useEffect 整段挪到 loadRecent 之下就好了。
- 又是经典稳态三步：PowerShell 杀 → cargo build → pnpm tauri dev。
  - 一个有意思的现象：cargo build 只用了 2.37s 没出现 Compiling 日志——后台的 tauri dev 一直在监听 lib.rs 变更，已经把它编过了。我额外加的 `close_repo` 是 dev 进程杀掉之后才改的，所以才要走手动 cargo build。stat 比对 src/lib.rs 和 exe 时间戳 + grep `close_repo` 验证了 exe 含最新符号，确证无遗漏。
- 验证流程：grep 验证二进制符号（`grep -c "close_repo" target/debug/gitmanage.exe`）—— 适用于最小改动确认是否真的进了二进制。

## 第十一轮：「最近 ▾」键盘导航

把鼠标下拉做成 power-user 工具——↑↓/Enter/Esc 全键盘可用。

**前端改动（App.tsx）**：
- 新 state `recentMenuIndex: number`，初始 0
- 新 effect：菜单打开期间监听 `document keydown`：
  - `ArrowDown`/`ArrowUp` 在 recent 数组里循环移动（环形）
  - `Enter` 取最近项，等价点击逻辑（不存在/当前仓库都正确处理）
  - `Escape` 关菜单
  - 每次 index 变化后 `scrollIntoView({block: "nearest"})` 把高亮项滚动到可视区
- 触发按钮 onClick：切换菜单时同步把 index 初始化到当前仓库所在索引（找不到就 0）
- 每项 onMouseEnter 同步 index（鼠标键盘状态统一）
- 每项加 `data-idx` 属性供键盘 effect 通过 querySelector 拿到 DOM
- 菜单底部 title 行右侧加 hint: 「↑↓ 选择 · Enter · Esc」
- 触发按钮 title 改为「切换到最近打开的仓库（↑↓ 选择 · Enter 打开 · Esc 关闭）」

**CSS 改动（App.css）**：
- `.recent-menu-title` 改为 flex + space-between，让 title 和 hint 并列
- `.recent-menu-hint`：等宽字体、不大写、降透明度
- `.recent-menu-item.active`：outline 1px accent-2 + 背景 panel（不挤占布局）
- `.recent-menu-item.active.current`：高亮 + current 同存时仍显蓝底

**只动前端**：tsc 通过；tauri dev Vite HMR 直接热更，无需重启。这次没动 Rust。

## 第十二轮：把「打开仓库」按钮改成「打开目录」

用户反馈：截图里主视图下蓝色「打开仓库」按钮 + 旁边「刷新」组合得让人困惑——既已开仓库了它还在。开仓库后**复用**这个位置，改成「打开目录」=在 Windows 资源管理器里打开当前仓库目录，复用上一轮的 `reveal_in_explorer` Rust 命令。

**前端改动（App.tsx）**：
- 「打开仓库」按钮：包 `{!repo && (...)}`，仅欢迎页显示（输入框 Enter 仍能直接打开，不影响路径输入流程）
- 新「打开目录」按钮：包 `{repo && (...)}`，onClick 调 `reveal_in_explorer(repo.path)`，title 写明「在 Windows 资源管理器中打开当前仓库目录」
- 「刷新」「最近 ▾」「关闭」位置不变

**CSS 不动**：新按钮继承默认主按钮样式（蓝底白字），与欢迎页的「打开仓库」按钮视觉一致。

**未动 Rust**：tsc 通过、Vite HMR 热更，无需重启。

**用户确认的可见性矩阵**（已通过 AskUserQuestion 二次确认）：
- 欢迎页：「打开仓库」主按钮（蓝）
- 主视图：「打开目录」主按钮（蓝）+ 刷新 ghost + 最近 ▾ ghost + 关闭 ghost
- 「打开目录」仅在 `repo` 存在时出现，缺失路径会走 Rust 端 `Path::new(...).exists()` 检查并返回 error，前端 setError 弹条

## 第十三轮：路径输入框变统一入口（方案 2）

用户选了方案 2：已开仓库时顶部路径输入框改为只读「仓库入口」，点击弹统一菜单；原「最近 ▾」按钮功能并入后移除（减一个按钮）。

**交互设计**：
- 欢迎页：输入框照旧可编辑，Enter 打开（完全不变）
- 已开仓库：input readOnly + `--panel-2` 底色 + pointer 光标，视觉上是"面包屑"；点击弹菜单
- 菜单结构：当前仓库信息头（📁 名称 + mono 路径）→ 三个动作（📂 在资源管理器中打开 / ⧉ 复制路径 / ✏️ 输入其他路径…）→ 分隔线 → 「切换到」最近列表（沿用 ↑↓/Enter/Esc 键盘导航 + ● 当前高亮 + missing 灰显）→ 分隔线 → ✕ 关闭当前仓库
- 「输入其他路径…」：setPathEditing(true) → 关菜单 → requestAnimationFrame 后 focus + select 全选（不 rAF 会被 readOnly 拦住）；Enter 提交，Esc 还原 repo.path 退出编辑态
- 键盘导航 effect 锚点从旧按钮换到 pathMenuRef（现在是包输入框的容器），逻辑不变

**State 重命名**：recentMenuOpen→pathMenuOpen、recentMenuRef→pathMenuRef（语义更准）；新增 pathEditing、repoInputRef

**顺带修的既有 bug**：openRepo 失败时 setRepo(null) 退回欢迎页，但 Rust 侧还持有上一个仓库的句柄（文件锁残留）——catch 里补 `invoke("close_repo").catch(()=>{})` 主动释放

**遇到的怪事**：openRepo 和 closeRepo 里各发现一处我没写过的 `setPathEditMode(...)` 调用（state 从未声明，tsc 必炸）——来源不明（可能上一轮编辑意外写入或手工动过），已替换为正确的 setPathEditing

**CSS**：`.path-menu-wrap`（relative + flex:1）、`.repo-input[readonly]`（pointer + panel-2 底）、`.path-menu`（left:0 覆盖 right:0，480px 宽）、`.path-menu-head/-name/-sub`

**只动前端**：tsc 通过 + Vite HMR 热更。最终顶栏：Logo / [路径入口] / 浏览… / 打开目录(蓝) / 刷新 / 关闭 / ⎇分支 / fetch·pull·push·Console

## 第十四轮：输入框彻底降级为纯展示

用户决策：「不用输入路径，因为有浏览直接打开。这个输入框，只做显示项目路径」——彻底去掉手输路径的能力，交互聚焦。

**改动（App.tsx）**：
- input 永远 `readOnly`，去掉 onChange/Enter/Esc 编辑逻辑；已开仓库时加 `.has-repo` class（pointer 光标 + hover 高亮），欢迎页只给 panel-2 底色不给 pointer（点了没动作）
- placeholder：`点「浏览…」选择仓库目录，或从最近打开中选择`
- 删 state：pathEditing、repoInputRef
- `openRepo(path)` 签名改为**必须显式传路径**（所有调用点：browseFolder / 欢迎页最近列表 / 菜单最近列表 / 欢迎页示例链接，本来就都传了显式路径）
- 菜单删「✏️ 输入其他路径…」项
- 删欢迎页「打开仓库」主按钮（没有输入可确认了）；「浏览…」在欢迎页升级为主按钮样式（蓝），已开仓库时退回 ghost
- 欢迎页文案：「输入仓库路径开始」→「选择仓库目录开始」，hint 同步改

**CSS**：`.repo-input[readonly]`（panel-2 底色）、`.has-repo` 才有 pointer + hover accent 描边

**开仓库的最终入口**（只有两个）：
1. 顶栏「浏览…」弹系统文件夹选择器（支持仓库内任意子目录，Rust discover 向上找 .git）
2. 最近列表（欢迎页列表 / 显示框菜单里的「切换到」）

**只动前端**：tsc 通过 + Vite HMR 热更。gitmanage.exe 48MB 运行中。

## 第十五轮：修 reveal_in_explorer + 删「打开目录」按钮

用户反馈（附截图）：菜单里「📂 在资源管理器中打开」没有打开对应目录；且既然菜单能开，顶栏「打开目录」按钮就多余了。

**根因**：`explorer /select,<目录>` 的语义是「打开**父目录**并选中该项」——对 `C:\code\agent-hub` 会停在 `C:\code` 只高亮 agent-hub，用户视角就是"没打开对应目录"。

**Rust 修复（lib.rs）**：
- 目录 → `explorer <dir>` 直接进入该目录
- 文件 → 保留 `/select,<file>`（打开所在目录并选中文件，这个语义是对的）
- 注释里写明这个坑，防止以后改回去

**前端（App.tsx）**：删掉顶栏「打开目录」按钮块（第九轮加的，被菜单项取代）。顶栏最终形态：Logo / [路径显示框] / 浏览… / 刷新 / 关闭 / ⎇分支 / fetch·pull·push·Console

**编译**：Rust 动了 → 稳态三步走完（cargo build 1m49s），新 exe 已起。

**经验**：`explorer /select,X` 停在父目录只高亮不进入——对"打开这个目录"的需求必须用 `explorer X`。/select 只适合"在文件夹里定位某个文件"。

## Git 收尾提交

- `0fa2289` feat: 仓库统一入口菜单 + 失效检测 + 关闭仓库（3 文件，+595/-49，第九至十五轮代码净变更一个提交——中间轮次相互改写，拆分无意义）
- `6b8202c` docs: 记录第九至十五轮开发日志
- 仓库无 remote（纯本地），无需 push；工作区干净。

## 排障：菜单「在资源管理器中打开」无反应（21:10）

- 现象：点统一入口菜单的「📂 在资源管理器中打开」没有打开目录。
- 根因：运行中的 target/debug/gitmanage.exe 是 20:52 的旧构建，而 reveal_in_explorer 命令 20:55 才随 0fa2289 提交加入——前端靠 Vite 热更新是新菜单，后端二进制是旧的，invoke 直接失败。
- 验证：用 Python Popen(["explorer", dir]) 模拟 Rust spawn 逻辑实测能正常打开目录，机制无问题。
- 结论：重启 pnpm tauri dev 让后端重编即可。教训：Tauri dev 若中断或 exe 直接运行，前后端版本可能脱节，排查"前端有入口点了没反应"先对比 exe 构建时间与相关提交时间。

## 修复：reveal_in_explorer 正斜杠路径打开错目录（21:45）

- 现象：点击后打开的是「文档」而非仓库目录。
- 根因：repo.path 存的是正斜杠形式（C:/Users/...），explorer.exe 命令行解析把 / 视为开关前缀（如 /select），正斜杠路径被判为无效参数，静默退化为打开默认文件夹。
- 修复：lib.rs reveal_in_explorer 入口先 path.replace("/", "\\\\") 再 spawn；cargo build 通过（33.8s）。
- 教训：Windows 上给 explorer.exe 传参必须用反斜杠路径；Path::display() 不会归一化分隔符，原样输出。
